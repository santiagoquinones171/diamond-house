import { Component } from '@angular/core';

@Component({
  selector: 'app-footer',
  standalone: true,
  imports: [], // No necesitas importar nada para usar @for
  templateUrl: './footer.html',
  styleUrl: './footer.css'
})
export class Footer {
  // No necesitas ninguna lógica aquí para las partículas
}